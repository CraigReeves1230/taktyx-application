<h1>Editing Photo</h1>
<%= image_tag @photo.image.thumb.url %><br><br>
<%= form_for @photo do |f| %>
    <%= f.label :description %><br>
    <%= f.text_area :description %><br><br>
    <%= f.submit "Update Photo" %>
<% end %>

<%= link_to 'Back', photos_path %>
